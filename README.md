   - To search for dorks related to a specific domain:
     ```
     bash dorks.sh -d <domain>
     ```
   - To search for dorks using a file containing multiple domains:
     ```
     bash dorks.sh -f <file_path>
     ```
